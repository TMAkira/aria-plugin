---
description: Initialize the .aria/ project knowledge directory — experimental (aria:setup skill)
---

Invoke the `aria:setup` skill via the Skill tool, forwarding the arguments below as the skill `args`.

Arguments: $ARGUMENTS
