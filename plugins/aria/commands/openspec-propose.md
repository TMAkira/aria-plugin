---
description: OpenSpec — propose a new change with all artifacts in one step (aria:openspec-propose skill)
---

Invoke the `aria:openspec-propose` skill via the Skill tool, forwarding the arguments below as the skill `args`.

Arguments: $ARGUMENTS
