---
description: Set up an isolated worktree for feature work (aria:worktree skill)
---

Invoke the `aria:worktree` skill via the Skill tool, forwarding the arguments below as the skill `args`.

Arguments: $ARGUMENTS
